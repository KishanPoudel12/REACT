let a=1;
let b='1';


console.log(NaN ===NaN)   //NaN is not even equal to NaN (Amazing Right )

//so What to do to check a value is NaN or not ? 
//so for that we use isNaN function
console.log(isNaN(a)) //false

let c=NaN
console.log(isNaN(c))